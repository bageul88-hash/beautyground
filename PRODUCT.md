# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**주 고객 — 라이브로 유입되는 신규 온라인 고객.** 매장을 모르는 상태에서 라이브 방송·SNS를 통해 처음 들어온다. 첫 접점이 온라인이므로 브랜드 신뢰와 구매 확신을 화면 안에서 처음부터 만들어내야 한다. 쇼핑 화면(`/app/*`)은 모바일 사용을 전제로 설계돼 있다.

그 외 실제로 서비스를 사용하는 역할(각각 별도 로그인·전용 화면 보유):

- **파트너(입점 브랜드사)** — `partners`. 상품·라이브·주문·정산을 직접 관리. 온보딩은 입점 신청(`partner_applications`) → 승인.
- **진행자(라이브 호스트)** — `hosts`. 뷰티그라운드가 모집한 전문 진행자로, 여러 브랜드를 오가며 방송하고 판매 실적 등급에 따라 수수료를 받는다. 브랜드가 자체 방송하는 경우(`lives.host_id`가 없음)에는 진행자 수수료가 발생하지 않는다.
- **관리자** — `app_admins` + `is_admin()` RPC. 홈 화면 큐레이션, 입점 신청 심사, 진행자 관리, 수수료 등급·진행자 정산 관리.

## Product Purpose

백화점 뷰티 편집샵 뷰티그라운드의 온라인 판매 채널. 오프라인 매장에서 다루는 브랜드를 온라인 커머스 + 라이브커머스로 판매하고, 입점 브랜드사에게는 진열·방송·판매·정산까지 이어지는 유통 경로를 제공한다.

성공은 두 축으로 정의된다: (1) 라이브를 통해 처음 들어온 고객이 구매까지 완료하고 재방문하는 것, (2) 입점 브랜드사가 이 채널을 통해 실제 매출을 만들어 계속 남는 것.

## Positioning

**실제 백화점 매장을 가진 편집샵.** AK플라자 광명점·수원역점에 실물 매장을 운영하며, 온라인 전용 경쟁 서비스가 복제할 수 없는 신뢰·체험 기반을 갖는다. 온라인 화면은 이 오프라인 실재성을 근거로 삼아야 하며, 매장 없는 셀렉트샵처럼 보이면 핵심 자산을 버리는 것이다.

## Operating Context

- **구매 흐름**: 홈/카테고리/브랜드 탐색 → 상품 상세 → 장바구니(비로그인 게스트 장바구니 지원, 로그인 시 서버 장바구니로 병합) → 주문/결제 → 주문 내역·취소 요청.
- **라이브 흐름**: 라이브 목록 → 시청(실시간 채팅·하트·시청자 수) → 방송 중 판매자가 지정한 "지금 판매" 상품·공지핀 노출 → 라이브 쿠폰 적용 → 구매. 종료 후 다시보기.
- **파트너 흐름**: 입점 신청 → 승인 → 상품 등록(수기 입력 또는 URL/상품명 기반 자동 수집) → 라이브 편성 → 주문 처리·배송 정보 입력 → 정산.
- **진행자 흐름**: 등록 → 승인 → 방송 진행 → 판매 실적 확인 → 등급 기반 정산. 진행자 화면은 구매자 개인정보를 보지 않는다.
- **오프라인 연계**: 매장 방문 고객, 백화점 멤버십(AK플라자 등) 연계 UI가 마이페이지에 존재한다.
- **정산 구조**: 파트너는 수수료율 기반, 진행자는 판매액 구간별 수수료 등급(`commission_tiers`) 기반. 정산 규칙 자체는 별도 ERP에서도 운영된다.

## Capabilities and Constraints

**구현되어 동작하는 기능**
- 인증: Supabase Auth — 이메일+비밀번호, 카카오 OAuth.
- 커머스: 게스트 장바구니(localStorage) + 로그인 시 서버 병합, 찜(위시리스트), 배송지 관리(기본 배송지), 주문 내역, 고객 취소 요청 → 판매자 승인 → 결제 취소·재고 복원.
- 결제: PortOne v2 카드결제. 서버에서 DB 가격으로 금액을 재계산해 검증(위변조 방지). 배송비 3,100원 / 20,000원 이상 무료.
- 라이브커머스: Cloudflare Stream 기반 송출·재생, 실시간 채팅, 하트, 시청자 수, 방송 중 하이라이트 상품·공지핀 실시간 동기화, 다시보기.
- 라이브 쿠폰: 정액/정률, 최소구매금액, 수량 한정. 원자적 발급·회수 RPC.
- 리뷰: 파트너 원본 쇼핑몰에서 수집한 리뷰를 읽기 전용으로 노출(작성자명 마스킹). **고객이 직접 리뷰를 작성하는 기능은 없다.**
- 파트너 상품 등록: 수기 입력 + URL 붙여넣기/사이트+상품명 자동 수집.
- 관리자 홈 큐레이션: 히어로 배너, 마퀴 문구, 카테고리 썸네일.
- 회원 등급: 누적 결제액 기반 BASIC/SILVER/GOLD/VIP.

**미구현 — 화면에는 보이지만 동작하지 않는 것(신규 작업 시 "있는 기능"으로 취급 금지)**
- 마이페이지의 쿠폰함·포인트·최근 본 상품·리뷰 관리: 이동할 화면이 없는 표시용 항목.
- 알림: 알림 아이콘·"알림 설정" 항목만 있고 알림 시스템 자체가 없음.
- 적립금(3% 표기, 카카오채널 5,000원): 실제 사용 처리 미구현.
- 마이페이지 백화점 멤버십 연동 상태는 하드코딩된 값.

**기술 제약**
- React 18 + TypeScript + Vite 6, react-router-dom v6(단일 라우터 파일 `src/App.tsx`, lazy loading 없음).
- 스타일: Tailwind CSS 3 + 전역 스타일시트 1개(`src/styles/globals.css`). CSS-in-JS·CSS 모듈 미사용, 외부 컴포넌트 라이브러리 없음.
- 백엔드: Supabase(Postgres + Auth + RLS). 마이그레이션은 `supabase/*.sql`을 수동 적용.
- 서버리스 API: Vercel `api/*.ts`.
- 배포: Vercel, `git push` 기반 자동배포(수동 `vercel link` 금지).
- 개발 서버: `npm run dev` (포트 5173).
- **i18n 없음.** 모든 UI 문구가 한국어 하드코딩이며 다국어 계획은 확정된 바 없다.
- CSP 설정 없음.
- **쇼핑 화면(`/app/*`)은 데스크톱에서도 480px 폭 모바일 프레임을 화면 가운데 띄우는 구조를 유지한다(확정 제약).**

**미확정(임의로 정하지 말 것)**
- 정식 오픈 시점 및 오픈 전까지 노출 범위.
- 적립금·쿠폰·알림의 실제 운영 정책.
- 다국어 지원 여부.

## Brand Commitments

- 상호: 주식회사 뷰티그라운드 / 대표 유성보 / 사업자등록번호 303-81-80858 / 통신판매업신고 제2022-성남분당-0349호 / 경기도 성남시 분당구 성남대로 345, 4층 407호 / 02-897-8287 / beautyground.official@gmail.com.
- 서비스명 표기: "뷰티그라운드". 로고 자산 `public/images/bg-logo-mark.png`, `bg-logo-icon.png`, `bg-logo-192.png`.
- **폰트는 맑은 고딕으로 통일(확정 지시).** 라이선스 문제를 피하기 위한 결정이므로, 임의로 다른 폰트를 도입하지 않는다. (참고: 현재 Noto Sans KR·Noto Serif KR import 구문이 코드에 남아 있으나 실제 폰트 스택에는 매핑되지 않은 잔재.)
- **통신판매중개자 면책문구를 넣지 않고, 뷰티그라운드가 통신판매업자로서 직접 책임지는 입장을 유지한다(확정 지시, PG 심사 이력에 근거).**
- **골드 계열(#b8924a 등)은 앞으로 배제한다(확정 지시, 2026-07-29).** 현재 코드에 골드·크림·다크 토큰이 남아 있으나 이는 교체 대상이며, 신규 작업에서 골드를 브랜드 색으로 사용하지 않는다. 대체 컬러 방향은 미확정.

## Evidence on Hand

**실제 자료**
- 쁘띠페(petitfee) 실제 상품 약 72종이 Supabase에 등록되어 있고, 원본 쇼핑몰에서 재수집한 실제 갤러리·상세 이미지가 연결돼 있다(`_scrape_automation/all-products.json`에 실제 UUID·원본 URL 기록).
- 실제 매장 사진 4장(`public/images/about/*.jpg`), 회사소개 PDF(`public/files/beautyground-company-intro.pdf`).
- 회사소개 페이지의 연혁(2022~2026), 로드맵, 실제 입점 브랜드명 목록.
- 리뷰 수집 파이프라인은 실제로 동작하며 화면에 렌더링된다. 다만 상품별 실제 리뷰 개수는 Supabase 현재 값으로 확인해야 하며, 코드 저장소에 고정된 수치는 없다.

**반드시 실제로 오해하면 안 되는 자료**
- `src/constants/index.ts`의 상품 16종(설화수·랑콤·라메르·샤넬 등), 라이브 6건, 브랜드 8곳, 채팅 11줄, 백화점별 브랜드 수 등은 **전부 데모용 가상 데이터**이며 실제 취급 브랜드가 아니다. B2B 마케팅 페이지·목업에만 쓰인다. 이 데이터를 실적·취급 브랜드로 표기하지 말 것.
- 마이페이지의 주문/찜/쿠폰/포인트 수치, 백화점 멤버십 연동 상태는 목업 값이다.
- 존재하지 않는 후기·고객사·수상·벤치마크·가격 정책을 만들어내지 말 것.

## Product Principles

1. **오프라인 실재성이 최상위 근거다.** 실제 백화점 매장이 있다는 사실이 신규 온라인 고객에게 신뢰를 만드는 유일한 자산이므로, 화면 어디서든 이 근거가 사라지면 안 된다.
2. **첫 접점은 라이브다.** 매장을 모르는 사람이 방송을 통해 처음 들어온다는 전제로, 브랜드·상품·구매 방법을 처음 보는 사람 기준으로 설명해야 한다.
3. **모바일이 기본 화면이다.** 쇼핑 경험은 480px 프레임 안에서 완결되어야 하며, 데스크톱 전용 레이아웃을 전제하지 않는다.
4. **미구현 기능을 구현된 것처럼 보이게 하지 않는다.** 알림·적립금·쿠폰함 등은 동작하지 않으므로, 새 작업에서 이를 살아있는 기능으로 확장하거나 전제하지 않는다.
5. **거래 주체로서의 책임을 감춘 표현을 쓰지 않는다.** 중개자 뒤에 숨는 문구 없이, 판매자로서 책임지는 서비스로 표기한다.

## Accessibility & Inclusion

- 문서 언어는 한국어(`<html lang="ko">`)로 고정.
- 아이콘 전용 버튼에는 `aria-label`, 현재 탭에는 `aria-current`, 장식 요소에는 `aria-hidden`, 폼 입력에는 `label` 연결이 이미 광범위하게 적용돼 있다(70개 파일). 이 수준을 후퇴시키지 않는다.
- 모션 최소화 선호(`prefers-reduced-motion`)에 대응해 마퀴 애니메이션을 중지하는 처리가 있다. 새 모션도 동일하게 대응해야 한다.
- 포커스 표시 토큰(`focus:shadow-focus`)이 존재하며 `focus:outline-none`과 함께 쓰인다 — 포커스 표시를 없애기만 하는 처리는 금지.
- 자동화된 접근성 검사 도구는 도입돼 있지 않다(제품 요구사항으로 확정된 표준 없음).
